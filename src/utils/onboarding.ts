/**
 * AsyncStorage key marking onboarding as completed.
 * Lived in screens/OnboardingScreen.tsx until that screen was removed
 * (superseded by the SportSelection → … → OnboardingComplete flow).
 */
export const ONBOARDING_COMPLETE_KEY = '@kinetic_onboarding_done';
